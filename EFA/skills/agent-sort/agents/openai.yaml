interface:
  display_name: "Agent Sort"
  short_description: "Evidence-backed EFA install planning"
  brand_color: "#0EA5E9"
  default_prompt: "Use $agent-sort to build an evidence-backed EFA install plan."
policy:
  allow_implicit_invocation: true
